vendor/

# a commercial ROM to test against, which is not ours to carry
_realRomTest/
_romFromHaxe/

# every build writes here and nothing else does
export/

tests/harness/.build/
tests/.build.log
tests/.observables.txt
tests/.template/
tests/.linked.txt
tests/.libmd.txt
tests/.run-copy.sh

*.o
*.bin
*.out

# the type checks, which write nothing they keep
tests/.hostcheck/
tests/.typed.n

# the player source generation the separation check reads
tests/.player/

# where the pad comparison puts the two ROMs it holds to each other
tests/.pad/

# the settings files the settings check writes and reads again
tests/.settings/
tests/opn2/.build/

# the gdb session the remote protocol check drives, and what the stub said while it ran
tests/.gdb/

# the resource pipeline check
tests/.resources/

# permission entries this tooling writes for itself
.claude/settings.json
