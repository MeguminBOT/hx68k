# the window's own behaviour, which needs SDL and so cannot live inside gate:
#
#     haxelib run hx68k build windowcheck
#
-cp src
-main hx68k.test.WindowCheck
-cpp ../export/md/tests/obj/windowcheck
