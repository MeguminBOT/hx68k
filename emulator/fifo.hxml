# Nemesis' VDP port access ROM, which is the specification C4's timing half is held to.
# neko bin/fifo.n <repository root> [pages] [directory for PNGs]
-cp src
-main hx68k.test.md.FifoCheck
-neko ../export/md/tests/bin/fifo.n
