# the resource pipeline, held to SGDK's rescomp where a JVM is present to compare against:
#
#     haxelib run hx68k build check
#     export/md/tests/bin/check <repository root>
#
-cp src
-main hxres.Check
-D md_runtime
-D analyzer-optimize
-cpp ../export/md/tests/obj/check
